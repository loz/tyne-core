<% cache ["v1", "projects", "new", current_user] do %>
  <%= simple_form_for(@project, :validate => true) do |f| %>
    <%= f.input :key %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.button :submit, :class => "btn btn-primary" %>
  <% end %>
<% end %>
